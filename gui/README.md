## Simple GUI for monitoring your remaining data

## Export 
```shell
pip install -U pyinstaller
```
```shell
PyInstaller main.py --name jahan-nama-gui --icon icon.ico --clean --onedir --noconsole
```

